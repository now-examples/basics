{
  "name": "now-example",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "bundle-now": "meteor build --architecture=os.linux.x86_64 ../bundle/ && cd ../bundle/ && split -b 4m src.tar.gz app. && rm src.tar.gz"
  },
  "dependencies": {
    "@babel/runtime": "7.0.0-beta.55"
  },
  "meteor": {
    "mainModule": {
      "client": "client/main.js",
      "server": "server/main.js"
    }
  }
}
